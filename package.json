{
  "name": "tts-sidecar-narrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Plugin de Claude Code: narración por voz con TTS-Sidecar.",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "build": "node build.mjs",
    "check-dist": "node build.mjs --check",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  }
}
