#!/bin/sh
echo "[pre-commit] Building dist/..."
npm run build
if [ $? -ne 0 ]; then
    echo "[pre-commit] Build failed, aborting commit."
    exit 1
fi
git add dist/
echo "[pre-commit] dist/ updated and staged."
